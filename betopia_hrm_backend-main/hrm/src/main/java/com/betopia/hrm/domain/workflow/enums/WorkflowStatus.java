package com.betopia.hrm.domain.workflow.enums;

public enum WorkflowStatus {
        DRAFT,
        PENDING,
        IN_PROGRESS,
        APPROVED,
        REJECTED,
        CANCELLED,
        ON_HOLD
    }